<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chupakubera.sqliteipkul">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo_round"
        android:roundIcon="@drawable/ic_logo_round"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.matkul.DaftarMatkulActivity"
            android:label="@string/home" />
        <activity
            android:name=".activity.jadwal.JadwalActivity"
            android:label="@string/jadwal" />

        <activity android:name=".activity.catatan.CatatanActivity"
            android:label="@string/catatan" />

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".activity.matkul.UpdateMatkulActivity"/>

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".activity.matkul.TambahMatkulActivity"/>

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".activity.jadwal.TambahJadwalActivity"/>

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".activity.matkul.TampilkanIpkActivity"/>

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".activity.jadwal.UpdateJadwalActivity"/>

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".activity.catatan.TambahCatatanActivity"/>

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".activity.catatan.UpdateCatatanActivity"/>

        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true"/>

    </application>

</manifest>